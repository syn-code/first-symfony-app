{% block navigation_section %}
<div class="contianer">
    <ul class="nav">
        {% for nav in navigation_links %}
            <li class="nav-item">
                <a class="nav-link" href="{{ nav.url }}">{{ nav.name }}</a>
            </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}

